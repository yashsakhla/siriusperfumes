import { FadeInOnScrollDirectiveDirective } from './fade-in-on-scroll-directive.directive';

describe('FadeInOnScrollDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeInOnScrollDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
